export interface Event {
   result: {
    eventId: string
    title: string
    city: string
    placeVenue: string
    date: string
    details: string
    category: string
    }
    message: string
    isSuccess: boolean
}
